apiVersion: v1
kind: ConfigMap

metadata:
  name: travel-config
  namespace: travel-planner

data:
  TEST_MODE: "false"
  APP_VERSION: "1.0.1"
  DATABASE_URL: "postgresql://postgres:postgres@postgres:5432/travel_db"