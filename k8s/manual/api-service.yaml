apiVersion: v1
kind: Service

metadata:
  name: api-service
  namespace: travel-planner

spec:
  selector:
    app: travel-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: ClusterIP